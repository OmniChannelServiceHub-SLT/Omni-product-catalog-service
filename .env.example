# Product Catalog and Inventory Service
# Port comes from the ports list the team shared (product: 3005)
PORT=3005
NODE_ENV=development

# From Omni_Channel_MongoDB_All_Service_ENV_Exact.pdf - Product Catalog & Inventory row.
# Same shared Atlas cluster/username/password as every other service, only the db name changes.
# Do NOT commit the real .env file - only this .example file goes to GitHub.
MONGODB_URI=mongodb+srv://OminiChannel:OminiChannel@cluster0.jhxpotk.mongodb.net/product_catalog_inventory_db?appName=Cluster0

JWT_ACCESS_SECRET=your-shared-jwt-secret
